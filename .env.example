# Konfigurim për zhvillim lokal. Kopjoje si `.env.local` dhe përshtate.
#
# KUJDES: në prodhim këto NUK lexohen nga ky skedar — Vite i pjek në build-time,
# ndaj imazhi i Docker-it e merr si --build-arg (shih etermini-deploy/.env).

VITE_API_URL=https://localhost:44345/api

# --- Vlera e prodhimit (referencë) ---
# VITE_API_URL=https://etermini-adminapi.troni.dev/api
